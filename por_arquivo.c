#include "baralho.h"
#include "ordenadores.h"

void ler_arquivo(){
    FILE *arquivo;
    fopen("TESTE.txt", "r");

    if (arquivo == NULL) {
        perror("Erro ao abrir o arquivo");
        exit(0);
    }

    int q_maos;



}